import sys
import deflate
import inflate

args = sys.argv[1:]
folder_for_archive = ""
archive_for_unpackage = ""
file_to_add = ""
file_to_rm = ""

# создание архива
for elems_of_input in range((args-2)):
    if args[elems_of_input] == "create":
        with open() as archiver:

# распаковка архива
for elems_of_input in range((args-2)):
    if args[elems_of_input] == "unpack":
        elems_of_input =
        with open() as archiver:

# добавление файла в архив
for elems_of_input in range((args-2)):
    if args[elems_of_input] == "add":
        with open() as archiver:

# удаление файла из архива
for elems_of_input in range((args-2)):
    if args[elems_of_input] == "rm":
        with open() as archiver: